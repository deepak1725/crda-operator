apiVersion: apps/v1
kind: Deployment
metadata:
  name: crda-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: crda-operator
  template:
    metadata:
      labels:
        name: crda-operator
    spec:
      serviceAccountName: crda-operator
      containers:
        - name: crda-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - crda-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "crda-operator"
